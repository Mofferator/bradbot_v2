# Errors

database_duplicate_entry = "database_duplicate_record"
insufficient_permissions = "insufficient_permissions"
missing_url = "missing_url"
invalid_url = "invalid_url"

# Permissions

add_image_perms = "add_images_permissions"